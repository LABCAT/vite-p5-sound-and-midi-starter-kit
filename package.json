{
  "name": "vite-p5-sound-and-midi-starter-kit",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "VITE_BASE_PATH=/vite-p5-sound-and-midi-starter-kit/ pnpm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@rollup/plugin-inject": "^5.0.5",
    "gh-pages": "^6.1.1",
    "sass": "^1.70.0",
    "vite": "^6.0.5",
    "vite-plugin-static-copy": "^2.2.0"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28",
    "p5": "^1.11.2"
  }
}